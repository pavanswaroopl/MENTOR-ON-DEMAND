This project was generated with Angular And Rest API.

MicroServices
Run the Microservices under the folder "MicroServices" and in the data base schema mentioned in the properties the tables are created and mapped according to the entity classes.

DataBase Info:
file "data" is included and run it for the pre required data.
The mentor id:harsha
mentor password: 12345678.

User id:user
User password:12345678.

Connecting Angular
Angular calls for the URL's that are necessary and the methods involved are implemented according to the necessity of the User.Login with the credentials given above and can browse through the facilities of this application.

Development server
First Run npm install and Run ng serve for a dev server. Navigate to http://localhost:4200/. The app will automatically reload if you change any of the source files.

Code scaffolding
Run ng generate component component-name to generate a new component. You can also use ng generate directive|pipe|service|class|guard|interface|enum|module.

Running unit tests
Run ng test to execute the unit tests via Karma.



